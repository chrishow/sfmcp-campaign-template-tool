{{! Example - use your actual template }}
<div class="my-widget">
  <img src="{{imageUrl}}" alt="Campaign Image" class="widget-image">
  <h2>{{formHeader}}</h2>
  <div>{{{formSubheader}}}</div>
  <input type="email" placeholder="Enter your email">
  <button>{{formButtonText}}</button>
  <div class="small-copy">{{{formSmallCopy}}}</div>
</div>
{{! Add elements for the 'thank you' state if needed }}
